# ============================================================================
# Valentina Bridge — Dependencias Python (pinned)
# Estación H2O · Maracaibo, Venezuela
# ============================================================================
# Instalar: pip install -r requirements.txt
# Python: 3.12+
# ============================================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- HTTP client (para llamar a Dify y Meta Graph API) ---
httpx==0.27.2

# --- Rate limiting ---
slowapi==0.1.9

# --- Observabilidad ---
prometheus-client==0.21.0       # métricas /metrics
python-json-logger==2.0.7       # structured logging JSON

# --- DB ---
# SQLite viene en la stdlib, no hace falta paquete extra.

# --- Telegram (alerts + kill switch) ---
python-telegram-bot==21.6

# --- Tests (solo dev) ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
